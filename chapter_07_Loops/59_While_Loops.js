// While Loops similar to the For Loop
// it has I, C, U -> initialisation will be outside of the loop

let attempts = 0;  // initialisation

while(attempts<3){  // condition
    console.log("Attempt" , attempts);
    attempts++; // upgrade
} 