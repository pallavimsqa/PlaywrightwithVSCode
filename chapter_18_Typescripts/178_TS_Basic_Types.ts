// to run the .ts file need ts-node to be installed - npm install -g ts-node typescript
// without ts-node - compile .ts to .js then run the .js file
let message: string = "Hello, Typescript";
let count:number = 42;
let isActive :boolean = true;

console.log("Message:", message);
console.log("Count:", count);
console.log("Is Active:", isActive);